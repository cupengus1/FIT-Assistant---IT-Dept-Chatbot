
// This file is now empty of data as the application uses a real database (Supabase).
// Types are imported to ensure no breaking changes if any other files reference types from here (though they shouldn't).
import { Procedure, Ticket, TicketStatus, StudentProfile } from './types';

// Constants for Mock Data have been removed.
